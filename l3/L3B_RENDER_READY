L3b HQ render trigger — retry 4

Inputs:
- O15 exact lossless WAV reconstructed from deterministic binary WavPack fixture v2
- M1 exact lossless qce-ng artifact

Pipeline:
O/M -> 88.2 kHz float64 F3-HQ -> unchanged OpenVoice V2 TCC
No post-L3 correction.

Retry 4 gates WavPack bytes and decoded O15 PCM samples. WAV container/header bytes are deliberately ignored.
